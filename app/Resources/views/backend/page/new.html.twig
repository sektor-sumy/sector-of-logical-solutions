{% extends ':backend:admin.html.twig' %}

{% block content %}
    <h1>Page creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin.page.index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
